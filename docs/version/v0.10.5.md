# leaferGame v0.10.5

## Version Positioning

`v0.10.5` closes the `0.10.x` delivery stage:

**Public API And Package Boundary Docs Closeout**

This version aligns public docs, stage links, and package-facing guidance with the completed `0.10.x` work.

---

## Product Boundary

This remains frontend 2D game engine package work.

`v0.10.5` does not add gameplay scope, editor UI, asset browser UI, visual authoring workflows, or new runtime APIs. It closes the package-boundary sprint so the next stage can focus on engine capability again.

---

## Goals

After `v0.10.5`, the project documentation should clearly explain:

- `0.10.x` has completed the package-facing API boundary baseline
- package entrypoints are documented and smoke-tested
- Node-safe imports and browser-facing imports are documented separately
- package artifact verification is available through `npm run verify:package`
- dodge-blocks now imports engine APIs through package-style entrypoints
- the repository remains an engine dependency package, not an editor product

---

## Scope

This version includes:

- README current-progress cleanup
- README implementation-status cleanup
- `v0.10.0` stage-plan link update
- roadmap update linking to this closeout pass
- public API docs stage-status note

This version does not include:

- new runtime behavior
- package export redesign
- npm publishing
- gameplay changes
- editor UI
- asset browser UI

---

## Semantics

- `0.10.x` is now treated as a completed Public API and Package Boundary sprint.
- Public API documentation remains package-facing guidance, not editor documentation.
- The next stage can start from a cleaner dependency-package baseline.

---

## Done When

- README says the project is current through `v0.10.5`.
- README says `0.10.x` has completed the package-facing API boundary baseline.
- `v0.10.0` links to `v0.10.5`.
- Roadmap links to `v0.10.5`.
- `npm run check`, `npm test`, `npm run build:example`, and `npm run verify:package` pass.
