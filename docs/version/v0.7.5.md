# leaferGame v0.7.5

## Version Positioning

`v0.7.5` is the final `0.7.x` cleanup pass:

**Selection-Aware Runtime Observability Docs And Sample Cleanup**

This version closes the selection-aware runtime observability sprint by aligning public docs, version links, and example descriptions with the current implementation.

---

## Goals

After `v0.7.5`, the project documentation should clearly explain:

- the project has progressed through `0.7.x`
- `0.7.x` stayed inside the frontend engine package boundary
- runtime tooling now supports entity selection
- selected entity details are visible in tooling output
- selected component details can use schema metadata
- the next stage can start from a clean documentation baseline

---

## Scope

This version includes:

- README current-progress cleanup
- README tooling/API wording updates
- `v0.7.0` stage plan update linking to this cleanup pass
- roadmap update linking to this cleanup pass
- dodge-blocks example docs alignment

This version does not include:

- new runtime behavior
- component editing
- form controls
- editor UI
- new gameplay

---

## Semantics

- `0.7.x` is now treated as a completed selection-aware runtime observability sprint.
- Runtime inspection work remains part of the engine package's read-only developer tooling.
- The package is still not an editor project.
- The next major stage can start with a fresh `v0.8.0` planning document.

---

## Done When

- README says the project is current through `0.7.x`.
- README describes runtime inspector selection and schema-assisted selected details.
- `v0.7.0` links to `v0.7.5`.
- Roadmap links to `v0.7.5`.
- `npm run check`, `npm test`, and `npm run build:example` pass.
