# leaferGame v1.0.1

## README And Package Docs Refresh

`v1.0.1` is a documentation-only patch release after the `v1.0.0` release
baseline.

It publishes the streamlined README to npm and keeps the detailed historical
progress narrative in `docs/project-history.md`.

## Scope

- update package metadata from `1.0.0` to `1.0.1`
- publish the optimized README to npm
- keep `1.0.0` as the runtime/API release baseline
- add no new runtime, framework, adapter, browser, or tooling API

## Verification

- `npm run check`
- `npm test`
- `npm run build:example`
- `npm run verify:package`
- `npm audit --audit-level=moderate`
