# leaferGame v0.6.5

## Version Positioning

`v0.6.5` is the final `0.6.x` cleanup pass:

**Runtime Tooling Docs And Example Integration Cleanup**

This version closes the runtime tooling panel sprint by aligning public docs, version links, and example descriptions with the current implementation.

---

## Goals

After `v0.6.5`, the project documentation should clearly explain:

- the project has progressed through `0.6.x`
- runtime tooling now includes formatted snapshots and browser panel sections
- dodge-blocks displays runtime debug, entity inspector, and component schema data
- version docs now live under `docs/version/`
- the next stage can start from a clean documentation baseline

---

## Scope

This version includes:

- README current-progress cleanup
- README public API/tooling updates
- roadmap link updates for the `docs/version/` directory layout
- `v0.6.0` stage plan update linking to this cleanup pass
- playbook path wording updated to match the current docs layout

This version does not include:

- new runtime behavior
- new tooling panel features
- schema-driven editing
- interactive inspector controls
- new example gameplay

---

## Semantics

- `0.6.x` is now treated as a completed runtime tooling panel sprint.
- Version docs are grouped in `docs/version/` to keep the root docs directory focused on evergreen docs.
- README should describe the current implementation rather than an older `0.4.x` milestone.
- The next major stage can start with a fresh `v0.7.0` planning document.

---

## Done When

- README says the project is current through `0.6.x`.
- Roadmap links resolve to `docs/version/*`.
- `v0.6.0` links to `v0.6.5`.
- The playbook recommends `docs/version/v*.md` for version docs.
- Documentation-only changes are committed cleanly.
